import { DesignDemo } from "@/components/DesignDemo";

/** §9.7.10 deliverable: one demo screen (nav + table + form with a focused input
 *  + one modal + one toast) proving every surface obeys the tokens — both themes. */
export default function DesignPage() {
  return <DesignDemo />;
}
